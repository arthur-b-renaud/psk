[package]
name = "psk-core"
description = "Orchestrator: recognizers -> overlap resolution -> fake-recognition guard -> vault."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
psk-secrets = { path = "../psk-secrets" }
psk-vault = { path = "../psk-vault" }
psk-verifiers = { path = "../psk-verifiers" }

[dev-dependencies]
psk-vault = { path = "../psk-vault" }
