[package]
name = "psk-cli"
description = "The `psk` binary: scan / proxy / hook / init / uninit / gain / test / top."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "psk"
path = "src/main.rs"

[dependencies]
psk-core = { path = "../psk-core" }
psk-init = { path = "../psk-init" }
psk-proxy = { path = "../psk-proxy" }
psk-secrets = { path = "../psk-secrets" }
psk-tui = { path = "../psk-tui" }
psk-vault = { path = "../psk-vault" }

anyhow.workspace = true
clap.workspace = true
reqwest.workspace = true
serde_json.workspace = true
tokio.workspace = true
