[package]
name = "psk-secrets"
description = "Detection rules for high-value secret patterns, compiled once into a RegexSet."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
psk-vault = { path = "../psk-vault" }
psk-verifiers = { path = "../psk-verifiers" }
regex.workspace = true

[dev-dependencies]
base64.workspace = true
serde_json.workspace = true
